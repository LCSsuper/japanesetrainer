import "react-range-slider-input/dist/style.css";
import { Box, Title } from "@mantine/core";

const Settings = () => (
    <Box>
        <Title>No settings</Title>
    </Box>
);

export default Settings;
